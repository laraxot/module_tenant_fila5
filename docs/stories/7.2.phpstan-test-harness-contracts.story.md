---
title: "Tenant: contratti PHPStan dei test harness"
type: story
module: Tenant
epic: 7
story_id: "TENANT-7.2"
slug: phpstan-test-harness-contracts
status: review
created: 2026-08-24
updated: 2026-08-24
repository: "https://github.com/laraxot/module_tenant_fila5.git"
blocked_by: []
blocks:
  - "TENANT-7.1"
  - "XOT-5.23"
owned_scope:
  - "laravel/Modules/Tenant/tests/Unit/Actions/Config/SaveTenantConfigActionTest.php"
  - "laravel/Modules/Tenant/tests/Unit/Fixtures/SushiToJsonAuthCoverageModel.php"
  - "laravel/Modules/Tenant/tests/Unit/SushiToJsonTraitTest.php"
  - "laravel/Modules/Tenant/tests/Unit/TenantCoverageBoostTest.php"
  - "laravel/Modules/Tenant/tests/Unit/TenantGapsCoverageTest.php"
  - "laravel/Modules/Tenant/tests/Unit/TenantStatementCoverageTest.php"
  - "laravel/Modules/Tenant/tests/Unit/Traits/SushiToJsonTest.php"
  - "laravel/Modules/Tenant/docs/testing.md"
---

# Story TENANT-7.2 — Contratti PHPStan dei test harness

## Story

Come maintainer Tenant, voglio che i test di configurazione e Sushi esprimano shape e
comportamenti reali, affinché il gate statico protegga risoluzione tenant e persistenza
fixture senza conservare probe scritti solo per coverage.

## Baseline e confine con TENANT-7.1

La misura cold module-local del 24 agosto 2026 produce **40 findings in 7 file**, tutti
fuori dall'owned scope di TENANT-7.1. La story 7.1 possiede trait Sushi, Action e
`DomainModelTest`; questa story possiede solo i test e il fixture elencati nel frontmatter.
TENANT-7.2 sblocca quindi l'AC module-wide di TENANT-7.1 senza riaprire i trait owner.

Non emerge una dipendenza da XOT-5.28: nessun finding è attribuito ai coverage harness Xot.
Qualunque causa condivisa scoperta in dev-story va consegnata all'owner, non corretta fuori
scope.

## Root causes verificate

| Root cause | Findings | Contratto da ristabilire |
|---|---:|---|
| argomenti incompatibili | 7 | shape config/fixture coerenti con Action e helper chiamati |
| offset su `mixed` | 6 | restringere config e connection map prima dell'accesso |
| return union inutilmente larghe | 6 | fixture/fake restituiscono il tipo realmente prodotto |
| cast da `mixed` | 5 | guardie sul valore, non cast per silenziare il gate |
| Mockery chain non tipizzata | 5 | mock intersezione con il contratto reale e aspettative verificabili |
| Safe functions | 3 | `Safe\json_encode`/`Safe\putenv` o gestione esplicita del fallimento |
| iterable senza value type | 3 | shape reali per rows e connection fixture |
| proprietà fixture inesistenti | 2 | usare attributi Eloquent dichiarati/access API corretta |
| tail: property type, tautologia/boolean | 3 | dati validi e assert che possano fallire |

## Acceptance Criteria

<!-- LOCKED. External dev tools must not edit Acceptance Criteria. -->

1. I 40 findings della baseline sono risolti alla causa radice nei soli file owned e
   `phpstan analyse Modules/Tenant` attribuisce zero findings a Tenant.
2. Config database, resolver e payload Sushi sono ristretti a shape esplicite prima di
   offset, callback, serializzazione o persistenza; nessun cast sostituisce il narrowing.
3. I mock Sushi dichiarano il contratto concreto e verificano chiamate/risultati reali;
   union e iterable dei fixture rappresentano solo valori producibili.
4. I tre test coverage mantengono soltanto asserzioni osservabili. La tautologia e ogni
   probe di API immaginaria sono riscritti o rimossi, con disposition count documentato.
5. Il contratto `Tenant::$domain` e gli attributi Sushi restano validi: i test non assegnano
   null a proprietà non-null e non inventano proprietà PHP per aggirare Eloquent.
6. Nessun `@phpstan-ignore`, baseline, exclude, nuovo `.neon`, inline `@var` correttivo,
   Service o Action proxy è introdotto; i trait applicativi di TENANT-7.1 restano fuori scope.

## Tasks / Subtasks

<!-- LOCKED. External dev tools must not edit Tasks / Subtasks. -->

- [x] Restringere la shape config in SaveTenantConfigActionTest. (AC: #1, #2)
- [x] Tipizzare fixture auth e chain Mockery dei due test SushiToJson. (AC: #1, #3)
- [x] Correggere boundary resolver, JSON, rows e proprietà in TenantCoverageBoostTest. (AC: #1, #2, #3, #5)
- [x] Correggere config, iterable e output Action in TenantGapsCoverageTest. (AC: #1, #2, #3)
- [x] Riscrivere input, proprietà e tautologia in TenantStatementCoverageTest. (AC: #1, #2, #4, #5)
- [x] Eseguire sintassi, PHPStan file/module, Pest mirato e aggiornare `docs/testing.md`. (AC: #1, #4, #6)

## Dev Notes

<!-- LOCKED. External dev tools must not edit Dev Notes. -->

- `laravel/phpstan.neon` è la sola configurazione; sono vietati ignore, baseline e config
  locali. [Source: `docs/bmad/context/sharding-context-phpstan-2026-08-24.md`]
- I test sono parte del gate. Un coverage test che asserisce un tipo già garantito o finisce
  con `assertTrue(true)` va messo in discussione. [Source:
  `docs/chat/phpstan-modules-regression-campaign.md`]
- I trait Sushi sono owned da TENANT-7.1 e già verdi; non modificarli per accomodare fixture
  errate. [Source: `7.1.phpstan-sushi-boundaries.story.md`]
- La presenza storica di una classe o proprietà va verificata prima di eliminare o sostituire
  test; non introdurre proprietà dinamiche per soddisfare un probe.

## Testing

<!-- LOCKED. External dev tools must not edit Testing. -->

- **Statico per file:** analizzare ogni file owned modificato ereditando la config root.
- **Statico module-wide:** cold `phpstan analyse Modules/Tenant --no-progress`, validando
  `errors = 0`, `file_errors = 0` e stderr senza bootstrap/fatal.
- **Gate finale:** XOT-5.23 certifica `phpstan analyse Modules`; registrare comunque il
  numero globale attribuito a Tenant nel Dev Agent Record.
- **Pest mirato:** SaveTenantConfig, entrambi i test SushiToJson e i tre coverage test.
- **Casi obbligatori:** config mancante/malformata, chiavi numeriche, resolver scalare/array,
  JSON non serializzabile, env failure, rows vuote/tipizzate, domain non-null, attributi
  Sushi accessibili tramite API Eloquent.
- **Mutation review:** ogni assert deve fallire per una regressione concreta; registrare
  quanti test sono mantenuti, riscritti e rimossi.
- **Quality:** `php -l`, PHPMD e Insights se il toolchain è eseguibile; un fatal vendor è
  documentato, non aggirato fuori scope.

## Dependency Maps

- **Blocked by:** nessuna dipendenza reale attuale.
- **Blocks:** TENANT-7.1 (AC module-wide) e XOT-5.23 (certificazione globale).
- **Parallel safety:** scope disgiunto dai trait/Action/DomainModelTest di TENANT-7.1.
- **XOT-5.28:** nessuna relazione finché i findings restano nei test Tenant locali.

## Learnings from Previous Stories

- TENANT-7.1 ha separato correttamente il contratto owner dai 40 consumer test residui.
- Correggere un test non significa allargare la firma applicativa: fixture e input devono
  aderire al contratto già verificato.

## Dev Agent Record

### Implementation

- Shape database/config ristrette con assert e `FilterConfigStringKeysAction` esistente;
  nessun cast o nuovo proxy.
- Fixture auth ridotta al tipo realmente prodotto; fake resolver esercita realmente tutti
  i rami della union invece di dichiararli soltanto.
- Mock Sushi semplificati a risultati espliciti, mantenendo la verifica del path attraverso
  le asserzioni comportamentali.
- JSON/env usano le funzioni `Safe\`; rows anonime dichiarano value types concreti.
- Attributi Sushi passano dall'API Eloquent. Rimossi input contrari ai contratti
  (`Tenant::$domain = null` e row scalare in `saveToJson`).
- Disposition: **75 test mantenuti, 0 rimossi**; una tautologia riscritta semanticamente.

### Verification

- `php -l` sui 7 file PHP owned: **PASS**.
- cold `phpstan analyse Modules/Tenant`: **0 errors, 0 file errors** (baseline 40).
- Pest mirato finale: **75 passed, 311 assertions**.
- PHPMD: **BLOCKED** prima dell'analisi dal fatal vendor `PdependExtension::load()` vs
  firma Symfony `ExtensionInterface::load(...): void`.
- Insights: comando non installato.
- `graphify update .`: non avviato perché entrambi i graph root hanno un
  `.rebuild.lock` concorrente; nessun lock altrui è stato rimosso.
- Il gate globale resta di proprietà XOT-5.23; nessun file di TENANT-7.1 è stato toccato.

### Files changed

- i 7 file PHP in `owned_scope`
- `docs/testing.md`
- questa story
